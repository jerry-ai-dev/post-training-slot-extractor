# Slot-Extractor Fine-tuning

Independent fine-tuning project for the appointment slot extractor described in `finetune-spec.md`.

The current repository stage is project scaffolding. See `docs/project-structure.md` for the directory layout and ownership of each module.

